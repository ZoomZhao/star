# 更新与数据保护

- 用户明确要求：更新、部署和新增预制任务不得覆盖任何既有的用户修改。保护全部规则版本、每日任务快照、自定义名称/科目/星星/次数上限/安排/启停状态，以及提交记录、账号和账本。
- 新任务升级只能补充指定的新任务；不得对既有孩子重新初始化整套默认模板。不得以默认配置覆盖、删除或重建已有记录。
- 部署前保存完整一致的生产备份。部署后按记录 ID 核对既有规则和每日任务的全部字段，同时核对账号、提交、账本等数据；新增字段仅允许明确迁移的默认值。发现差异时先判断是否为用户并发操作，不能自动用旧备份覆盖新数据。
- 用户报告设置回退时，先对比部署前备份、当前规则所有版本、生效日期和每日任务快照。模板从明天生效，当日快照可能仍显示旧值；不能据此认定数据丢失，也不能擅自改写历史快照。
- 发布验证需覆盖自定义任务规则（例如每日上限 10 次）在升级后的保留情况。
